name: Shell Commands

on: [push]
env:
   envName: stage
jobs:
  run-shell-command:
     runs-on: ubuntu-latest
     steps:
       - name: echo a string
         run: echo "Env name= ${envName}"
       - name: multi line 
         run: | 
           hostname
           env|sort
