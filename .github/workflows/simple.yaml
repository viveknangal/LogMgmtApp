name: Shell Commands

on: [push]

jobs:
  run-shell-command:
     runs-on: ubuntu-latest
     steps:
       - name: eco a string
         run: echo "hello world"
       - name: multi line 
         run: | 
           hostname
           env|sort
